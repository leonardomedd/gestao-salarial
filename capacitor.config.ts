import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.leonardomedd.gestaosalarial",
  appName: "Gestão de Salário",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
